# Execution Plan

1. Inspect existing Tailwind tokens and component primitives; define shared workspace and state styles.
2. Add typed model-settings client/schema/page and protected route.
3. Refactor layout and existing components incrementally, preserving all callbacks and streaming behavior.
4. Run TypeScript build and manually verify desktop/narrow layouts plus interaction states.
