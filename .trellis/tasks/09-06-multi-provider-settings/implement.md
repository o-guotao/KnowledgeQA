# Execution Plan

1. Add crypto dependency/configuration and a reversible Alembic migration.
2. Add ORM, schemas, encrypted-key service, URL validation, ownership-enforced API routes and API tests.
3. Generalize the streaming provider client and resolve active configuration in chat/cost paths.
4. Add frontend schemas/client operations after the API shape is stable.
5. Verify migrations, unit/route tests, error handling, key masking and the existing chat stream.
