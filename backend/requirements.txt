# Web 框架：REST API 路由、依赖注入
# 使用位置：app/api/*.py（全部路由）、app/core/security.py、app/core/errors.py
fastapi~=0.115.0

# ASGI 服务器，运行 FastAPI 应用；[standard] 含 httptools/uvloop 加速
# 使用位置：docker-compose.yml `uvicorn app.main:app`、本地 `uvicorn app.main:app --reload`
uvicorn[standard]~=0.30.0

# ORM + 异步数据库访问
# 使用位置：app/db.py（引擎/会话）、app/models、app/services/*.py、app/api/*.py、worker/main.py、scripts/seed.py、eval/*.py
sqlalchemy[asyncio]~=2.0.36

# PostgreSQL 异步驱动（database_url = postgresql+asyncpg://...）
# 使用位置：app/config.py、app/profiles.py 的 database_url，被 app/db.py create_async_engine 使用
asyncpg~=0.29.0

# SQLite 异步驱动（本地/测试降级模式，URL 规范化为 sqlite+aiosqlite:///）
# 使用位置：app/db.py
aiosqlite~=0.20.0

# 数据库迁移工具
# 使用位置：alembic/env.py、alembic/versions/0001~0011 全部迁移脚本（启动时 alembic upgrade head）
alembic~=1.13.0

# PostgreSQL 向量扩展的 SQLAlchemy 类型（向量检索/RAG）
# 使用位置：app/storage_compat.py（Vector 列）、alembic/versions/0001_init.py
pgvector~=0.3.6

# 数据校验与序列化模型
# 使用位置：app/schemas/*.py（请求/响应模型）、app/config.py
pydantic~=2.9.0

# 环境变量/配置加载（BaseSettings）
# 使用位置：app/config.py（Settings）
pydantic-settings~=2.6.0

# 异步 HTTP 客户端（调用外部 LLM/Embedding API）
# 使用位置：app/services/deepseek.py、app/services/openai_compatible.py、app/services/embeddings/openai_backend.py、eval/run_eval.py
httpx~=0.27.0

# SSE 推送（聊天流式输出）
# 使用位置：app/api/chat.py（EventSourceResponse）
sse-starlette~=2.1.3

# JWT 签发与校验（登录鉴权）
# 使用位置：app/core/security.py
PyJWT~=2.9.0

# 密码哈希
# 使用位置：app/core/security.py
bcrypt~=4.2.0

# Fernet 对称加密（加密存储模型 API Key）
# 使用位置：app/services/model_configs.py
cryptography~=43.0.0

# 解析 multipart/form-data（文件上传）
# 使用位置：app/api/documents.py（UploadFile/Form）
python-multipart~=0.0.12

# 对象存储客户端（文档文件存取，S3 兼容）
# 使用位置：app/services/storage.py（懒加载 Minio）
minio~=7.2.0

# 本地向量化与重排模型（Embedding/Rerank 后端）
# 使用位置：app/services/embeddings/fastembed_backend.py、app/services/rerank.py（均懒加载）
fastembed~=0.4.0

# PDF 解析（文档切块）
# 使用位置：app/services/chunking.py（懒加载 PdfReader）
pypdf~=5.0.0

# BM25 关键词排序（混合检索的关键词一路）
# 使用位置：app/services/bm25_index.py
rank-bm25~=0.2.2

# 中文分词（BM25 语料与查询的分词）
# 使用位置：app/services/bm25_index.py
jieba~=0.42.1

# LLM 调用链路追踪/观测
# 使用位置：app/services/langfuse_tracing.py（懒加载 Langfuse）
langfuse~=2.60.0
